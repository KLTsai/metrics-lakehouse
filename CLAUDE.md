## Agent skills

### Issue tracker

Issues live in GitHub Issues (KLTsai/metrics-lakehouse), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at the repo root (created lazily as needed). See `docs/agents/domain.md`.
