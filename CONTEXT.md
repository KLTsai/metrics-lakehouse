# metrics-lakehouse

多租戶 CSV 由 Drive 經 Airflow 入倉(Postgres)、dbt 建模、Spark 規模驗證的 lakehouse 學習專案。本詞彙表固化跨層溝通的共同名詞;定案於 Phase 1 grilling(2026-08-26 起),隨建構持續增補。

## Language

### 日期

**檔案日**:
檔名上的日期(`{table}_{YYYY-MM-DD}.csv`),代表該檔屬於哪一天交付;一列資料的檔案日 = 它所在檔案的日期。
_Avoid_: 檔名日期、執行日期

**業務日期**:
列內日期欄位(發票日期、商機開放日等)所載的交易時間。L1(入倉)不解讀業務日期,那是 dbt(L3)的責任。
_Avoid_: 交易日期(泛稱)

**logical_date**:
Airflow 對一次 DAG run 的排程日——它是 run 的屬性,不是檔案的屬性。專案約定:logical_date = D 的 run 負責檔案日 = D 的檔案;對齊是設計出來的,兩詞不是同義。
_Avoid_: execution_date(Airflow 2.x 舊名)、把 logical_date 當檔名日期的別名

**歸屬日**:
主鍵內嵌的日期(`ORD-{租戶}-{YYYYMMDD}-{序號}`),代表這列原本屬於哪一天。正常列歸屬日 = 檔案日。
_Avoid_: 批次日、批次日期

**遲到列**:
檔案日 > 歸屬日的列——該搭 1/1 的車,實際搭上 1/3 的車;列本身完好,只是出現得比它所屬的日子晚(業界詞:late-arriving data)。它進倉之後,「1 月至今」這類報表數字會比早先跑的大,這是資料晚到的正常現象,接受、不另做機制。
_Avoid_: delay、延遲(那是處理延遲/latency 的語境)

**一天的資料**:
同一檔案日的所有列;遲到列跟著它所在的檔案走。「刪除任意一天的資料」(T1.5)即刪除一個檔案日對應的分區。
_Avoid_: 按歸屬日或業務日期圈定「一天」

「批次」不作專案名詞:任何需要精確的句子(任務卡、驗證標準、ADR)改用檔案日/歸屬日/分區;日常口語(批次處理)不禁止。「每 N 列切一包」的效能手段叫「分塊(chunking)」,本專案未使用。

### 倉庫

**分區**:
倉庫刪補的單位 = (租戶, 檔案日),一個分區裡有兩張表;loader 對兩張表分開處理,實際每次刪補與交易的粒度是 (租戶, 表, 檔案日)(見 ADR 0002「已知邊界」)。重載一個分區 = 整個分區換新,不與既有資料合併(replace,非 upsert)。

**上傳契約層**:
客戶上傳檔的原始欄位長相(中文表頭、藍本 schema 逐欄照抄);產生器只生成此層,衍生欄位屬 dbt。詳見 ADR 0001。
_Avoid_: 落地層欄位混入產生器

### 漂移

**欄位名稱漂移**:
上游交付檔的欄位「名稱/組成」偏離契約(改名、增欄、減欄)。藍本真實事故即此類(欄位被 silently drop);L1 驗證(T1.3)負責擋下並告警。
_Avoid_: 表頭漂移、結構漂移、欄位漂移(單用「欄位漂移」分不清名稱與值)

**欄位值漂移**:
欄位「值的寫法」偏離契約(如 `2024年3月14日`、千分位數字)。由資料測試(T2.4)抓。
_Avoid_: 格式漂移(ADR 0001 的舊稱)

**分布漂移**:
資料統計分布偏離設計時的假設(ML 監控語境的 data drift)。本專案不處理;保留此詞條是為了避免「資料漂移」被拿來泛指以上任何一種。
_Avoid_: 資料漂移(泛稱)

拼字統一用「漂移」(語音輸入常誤植為「飄移」)。

### 建模

**租戶**:
平台的服務對象——一家使用本平台的公司就是一個租戶(現有 alpha、beta)。`tenant_id` 由入倉的 loader 賦值、不來自上傳檔,永遠可信。
_Avoid_: 客戶(指租戶時)、用戶、使用者

**客戶**:
租戶自己的交易對象,只存在於租戶上傳的資料裡(`customer_id`);是上游欄位,可能缺值。客戶只在它所屬的租戶內有意義:兩個租戶各有一個 C00001,不視為同一家。本專案「客戶」只指這一層;要說平台的客戶,一律說「租戶」。
交易明細與應收帳款兩表的客戶是兩群人(代號空間不同、上游無主檔可對應),不能按客戶把兩表對到一起;各自有自己的客戶維度。
_Avoid_: 把平台的服務對象叫客戶

**業績日**:
一張訂單被算進哪個月業績的那個日期 = 商機關閉日,且訂單狀態為成交(沿藍本)。開放日只是開始談,那天東西還沒賣出去。
_Avoid_: 用商機開放日算業績、把檔案日當報表角度

**未知客戶**:
上游沒填客戶代號的那些交易,在報表上統一歸到的一個客戶成員,代號沿用藍本的「其他」。例:1 月 1,050 列交易裡 50 列沒填客戶、合計 5 萬;若不歸到「其他」,「按客戶」的合計是 95 萬、「全部」是 100 萬,差的 5 萬報表上看不出去了哪裡;歸到「其他」後兩邊都是 100 萬,且看得到有 5 萬是不知道誰的。fact 建表時把缺值換成「其他」,客戶維度手動多放一列「其他」;交易明細與應收帳款各接自己的客戶維度,所以兩張維度各有「其他」,互不相干;鍵含租戶,「其他」是每租戶各一列。
_Avoid_: 把缺值列直接丟掉、NULL(在報表語境)

**未知日**:
上游沒填日期欄(商機開放日、發票日、實際入帳日等)的那些列,在日曆表上歸到的那一天,鍵用 `1900-01-01`。與未知客戶同一個道理:它讓「按月」的加總等於「全部」的加總,報表上 1900 年那一列就是缺日期的部分。
_Avoid_: 缺日期的列留 NULL 接日曆表、把 1900-01-01 當真實日期解讀
